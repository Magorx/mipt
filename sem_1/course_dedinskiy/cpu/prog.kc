jmp main

root:
push 2 * rax
push -1 * rbx
push rdx
add
div
out
ret

main:
in
in
in

pop rcx
pop rbx
pop rax

push rbx * rbx
push -4 * rax * rcx
add
sqrt
pop rdx

call root

push -1 * rdx 
pop rdx
call root